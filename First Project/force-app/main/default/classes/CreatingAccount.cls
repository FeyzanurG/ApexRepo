public class CreatingAccount {
    public static Account createAccount(String name){
        Account newAccount = new Account(); //instance
        newAccount.name = name;
        return newAccount;
    // Contact contactForCompany = new Contact(); instance structure
    // Lead decemberLeads = new Lead();
    }
}